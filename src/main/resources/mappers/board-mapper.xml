<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">
    <insert id="insertBoard">
        insert into RESTARUANT (name, type, location, dayoff, representative, grade, evaluation, picture)
        values
        (#{name}, #{type}, #{location}, #{dayoff}, #{representative}, #{grade}, #{evaluation}, #{picture})
    </insert>
    <update id="updateBoard">
        update RESTARUANT
        set name=#{name}, type=#{type}, location=#{location}, dayoff=#{dayoff},
            representative=#{representative}, grade=#{grade}, evaluation=#{evaluation}, picture=#{picture}
        where seq=#{seq}
    </update>
    <delete id="deleteBoard">
        delete from RESTARUANT where seq=#{seq}
    </delete>
    <select id="getBoard" resultType="board">
        select * from RESTARUANT where seq=#{seq}
    </select>
    <select id="getBoardList" resultType="board">
        select * from RESTARUANT order by seq desc
    </select>
</mapper>


<!--        <beans xmlns="http://www.springframework.org/schema/beans"-->
<!--       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"-->
<!--       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">-->

<!--</beans>-->